# CloudMart frontend environment (copy to .env)
# Point at the backend LoadBalancer created by EKS (kubectl get service),
# or http://localhost:5000/api for local dev.
VITE_API_BASE_URL=http://localhost:5000/api
